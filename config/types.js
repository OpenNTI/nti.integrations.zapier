const events = {
    created: 'created',
    enolled: 'enolled',
    completed: 'completed',
};

const objects = {
    user: 'user',
    course: 'course',
};

module.exports = {
    events,
    objects
};
